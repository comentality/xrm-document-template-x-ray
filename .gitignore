bin/
obj/
.vs/
*.user
*.suo
# Local Claude state stays out; the skills are part of the project.
.claude/*
!.claude/skills/
.nuget-apikey
artifacts/
# The private XrmToolBox instance xtb.ps1 builds, including its connection and its token cache.
.xtb/
# Word owner files, left behind when a fixture is open in Word.
~$*
# Windows, shots and report from tests\slow.ps1, rewritten on every run.
tests/.slow/
